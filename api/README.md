# API Details

`api/main.py` is a compatibility wrapper around `pipeline_pred.api.main`.

Current endpoints:

- `GET /health`
- `GET /observations?series=WCESTUS1`
- `GET /predictions?series=WCESTUS1`
- `GET /predictions/latest?series=WCESTUS1`

